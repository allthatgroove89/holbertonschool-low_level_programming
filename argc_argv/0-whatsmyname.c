#include <stdio.h>

/**
 * main - entry point
 * description - prints name followed by a new line
 * @argc: argument count
 * @argv: argument vector
 * Return: is 0
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);

	(void)argc;

	return (0);

}
