#include "main.h"

/**
 * _strlen(char *s) - Returns the length of a string
 * @s: - Integet to be swapped.
 * Return - is 0
 *
 */

int_strlen(char *s)
{

	int lenght = 0;

	while (*s != '0\')
	{
	length++;
	s++;
	}
	return lenght;
}
